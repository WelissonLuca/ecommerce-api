const fs = require('fs');

module.exports = {
	async create(req, res) {
		Model.create(req.body)

		produto.push({
			name: "dsadad22s",
			produto: "sada2222sd",
    });
    return res.send('cheguei aqui');
	},
};
